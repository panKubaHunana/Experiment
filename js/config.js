// Fill in after deploying server/ (see server/README.md). Leave empty to run
// the app in pure client-only mode — it still works, but on iOS the hourly
// reminder only fires while the app is open (Safari suspends background JS).
// Example: 'https://experiment-push.<your-subdomain>.workers.dev'
export const PUSH_SERVER_URL = '';

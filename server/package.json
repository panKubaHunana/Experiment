{
  "name": "experiment-push-server",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: VAPID Web Push backend for the Experiment PWA (hourly reminders that reach installed iOS/Android home-screen apps even when closed).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate-vapid": "node scripts/generate-vapid.mjs",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "web-push-neo": "0.1.2"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
